\chapter{Implementazione del simulatore e delle sue componenti}
\label{ch:simulatore}
Questo capitolo descrive la struttura del simulatore e di tutte le sue componenti
dal punto di vista funzionale e implementativo.
Si apre con una breve descrizione delle sue funzionalit\`a e della sua composizione (Sezione~\ref{sec:composizione}). 
Vengono poi mostrate le relazioni tra le classi per mezzo di un
diagramma UML. Inoltre vi \`e anche una sezione dedicata all'implementazione effettiva delle 
modalit\`a multihop \emph{forwarding singolo} e \emph{forwarding aggregato} --- il contributo principale di questa tesi, esposto nel
Capitolo~\ref{ch:contributo} ---, che ne descrive le strutture dati, il funzionamento della pipeline di trasmissione
e i meccanismi di contenimento.
Di seguito vi \`e l'elenco delle sezioni inerenti a tutte le componenti sulle quali si sviluppa il capitolo:
\begin{enumerate}[leftmargin=1.4em]
 \item \hyperref[sec:simulatore]{\textbf{Architettura del simulatore}}
 \item \hyperref[sec:buoy]{\textbf{Modellazione della boa}}
 \item \hyperref[sec:multihop-impl]{\textbf{Implementazione delle modalit\`a multihop}}
 \item \hyperref[sec:channel]{\textbf{Funzionamento del canale}}
 \item \hyperref[sec:config]{\textbf{Parametri di configurazione}}
 \item \hyperref[sec:metrics]{\textbf{Raccolta delle metriche}}
\end{enumerate}
Il capitolo si chiude con una sintesi di quanto presentato.

% ---------------------------------------------------------------------
\section{Composizione del simulatore}
\label{sec:composizione}
Il simulatore presenta esclusivamente le funzionalit\`a necessarie per gli esperimenti finali: 
comportamento delle boe, funzionamento del canale, logica di beaconing e metriche. 
Presenta una code base dalle dimensioni relativamente modeste ed \`e facile da modificare.
Inoltre, per la riproducibilit\`a dei test, occorre semplicemente configurare i parametri
--- categorizzati secondo le componenti del simulatore --- presenti in un unico file di
configurazione (Sezione~\ref{sec:config}). Il simulatore \`e interamente scritto in linguaggio Python,
per la semplicit\`a della sua sintassi e per la sua popolarit\`a negli ambienti di ricerca.

% ---------------------------------------------------------------------
\section{Schema UML delle classi}
\label{sec:uml}
La Figura~\ref{fig:classes-uml} mostra il diagramma UML delle classi del simulatore. 
Di seguito vi \`e l'elenco delle componenti con annesse brevi descrizioni per ognuna:
\begin{itemize}[leftmargin=1.4em]
  \item \texttt{Simulator} \`e l'orchestratore dell'intera simulazione: gestisce la lista delle boe 
  (\texttt{Buoy}), imposta il canale (\texttt{Channel}), traccia le metriche
  (\texttt{Metrics}) e crea gli eventi (\texttt{Event}, ciascuno di tipo \texttt{EventType})
  che scandiscono le azioni che avvengono durante il tempo simulato;
  \item \texttt{Buoy} modella il singolo nodo all'interno della rete: ha uno stato interno (\texttt{BuoyState}),
  decide quando trasmettere tramite il proprio \texttt{BeaconScheduler}, crea i pacchetti
  (\texttt{Beacon}) e li trasmette (o inoltra) al canale;
  \item \texttt{Channel} \`e il mezzo trasmissivo condiviso: gestisce le comunicazioni attive, avvisa se il canale \`e occupato o meno e
  si occupa di effettuare le comunicazioni in broadcast dei nodi, 
  raccogliendone quindi gli esiti (consegne, collisioni, perdite) nelle metriche;
  \item \texttt{Metrics} accumula gli esiti riportati da canale e boe 
  (invii, consegne, collisioni, perdite, latenze) insieme a quelli ottenuti 
  dalle scansioni periodiche della topologia di rete (numero medio di vicini per nodo). 
  Grazie a questi dati raccolti calcola le metriche (Sezione~\ref{sec:eval-metrics}, Capitolo~\ref{ch:risultati}) e le esporta in formato CSV al termine della simulazione;
  \item \texttt{BeaconScheduler} implementa i tre protocolli di beaconing accennati nel
  Capitolo~\ref{ch:contributo} --- e descritti nel Capitolo~\ref{ch:risultati} --- (SBP, ADAB, ACAB) dietro un'interfaccia comune 
  che restituisce in output l'intervallo prima della prossima trasmissione;
  \item \texttt{Beacon} costituisce il pacchetto in transito all'interno del canale; inoltre restituisce la dimensione in bit delle sue componenti per dedurne 
  il tempo che occorre per trasmetterlo (Eq.~\ref{eq:beacon-size});
  \item \texttt{ConfigHandler} restituisce i parametri di configurazione (Sezione~\ref{sec:config}) necessari per l'inizializzazione delle classi.
\end{itemize}
Non vi sono dipendenze circolari nelle relazioni: le interazioni tra una componente e l'altra (ad esempio la boa che
trasmette sul canale o che schedula eventi presso il simulatore) avvengono tramite
callback iniettate durante la fase di inizializzazione, come descritto nella Sezione~\ref{sec:files},
al fine di mantenere l'incapsulamento delle classi.
\begin{figure}[p]
  \centering
  \makebox[\textwidth][c]{%
    \includegraphics[width=1.30\textwidth,height=1.00\textheight,keepaspectratio]{main/images/uml_classes.pdf}%
  }
  \caption[Diagramma UML delle classi del simulatore]{%
    Diagramma UML delle classi del simulatore. \texttt{Simulator} gestisce le boe e
    usa canale e metriche; \texttt{Buoy} crea i beacon e li trasmette sul \texttt{Channel}
    assecondando il proprio scheduler. Tutte le componenti leggono i parametri dal
    singleton \texttt{ConfigHandler} e condividono le definizioni di \texttt{Event}
    ed \texttt{EventType}.}
  \label{fig:classes-uml}
\end{figure}

% ---------------------------------------------------------------------
\section{Architettura del simulatore}
\label{sec:simulatore}
Il simulatore si basa su un'architettura a eventi discreti che riguardano componenti modulari,
quali:
\begin{itemize}[nosep]
    \item il simulatore: modellato nella classe \texttt{Simulator};
    \item il canale: modellato nella classe \texttt{Channel};
    \item le boe: modellate nella classe \texttt{Buoy}.
\end{itemize}
Nel corso della simulazione vengono accodati vari eventi --- la cui struttura \`e dettagliata nella Sezione~\ref{sec:event} ---
ordinati per tempo simulato. Per tutta la durata della simulazione,
stabilita in fase di configurazione, ciascuno di essi viene
estratto dalla coda e gestito da un handler specifico dell'entit\`a che lo riguarda
(una delle componenti elencate sopra), referenziata all'interno dell'evento stesso.

% ---------------------------------------------------------------------
\subsection{Comunicazione tra le componenti}
\label{sec:files}
Il simulatore \`e organizzato in moduli, ognuno con diverse responsabilit\`a quali: 
la gestione della coda degli eventi e della popolazione di boe, il mezzo trasmissivo, il comportamento del nodo, la
struttura del frame, i protocolli di beaconing, la lettura centralizzata della
configurazione e la raccolta delle metriche.

Le componenti appartenenti a livelli di gerarchia diversi comunicano a vicenda tramite \emph{callback}, assegnate in
fase di inizializzazione. Ogni boa riceve dal simulatore una callback --- che funge da metodo di interfaccia comune per tutte le classi --- con cui accodare i
propri eventi futuri, e dal canale due callback con cui rispettivamente ascoltare il
mezzo e trasmettere. Analogamente, canale e boe riportano i dati alle metriche per mezzo dello stesso sistema.
Questo mantiene le classi modulari e facilmente sostituibili per eventuali modifiche future.

% ---------------------------------------------------------------------
\subsection{Struttura degli eventi}
\label{sec:event}
Ciascun evento \`e composto dai seguenti attributi:
\begin{itemize}[leftmargin=1.4em, nosep]
    \item l'istante di tempo simulato in cui l'evento deve essere gestito;
    \item il tipo di evento;
    \item l'entit\`a destinataria, che eseguir\`a l'handler specifico per quel tipo di evento;
    \item eventuali dati in input per l'handler (ad esempio il beacon da ricevere).
\end{itemize}
Di seguito vengono elencati i tipi di evento previsti, con annesse le entit\`a
che competono a ciascuno:
\begin{enumerate}[leftmargin=1.6em,nosep]
  \item \texttt{SCHEDULER\_CHECK} (Boa): verifica periodica dello scheduler che regola la volont\`a di trasmissione;
  \item \texttt{CHANNEL\_SENSE} (Boa): ascolto del canale per capire se \`e libero o meno;
  \item \texttt{DIFS\_COMPLETION} (Boa): completamento dell'attesa DIFS;
  \item \texttt{BACKOFF\_COMPLETION} (Boa): termine del periodo di backoff;
  \item \texttt{TRANSMISSION\_START} (Boa): inizio della trasmissione di un beacon;
  \item \texttt{TRANSMISSION\_END} (Canale): fine della trasmissione;
  \item \texttt{RECEPTION} (Boa): ricezione di un beacon;
  \item \texttt{NEIGHBOR\_CLEANUP} (Boa): rimozione delle voci obsolete dalle liste dei vicini;
  \item \texttt{BUOY\_MOVEMENT} (Boa): aggiornamento della posizione delle boe mobili;
  \item \texttt{BUOY\_ARRAY\_UPDATE} (Simulatore): aggiunta o rimozione di boe;
  \item \texttt{AVG\_NEIGHBORS\_CALCULATION} (Simulatore): campionamento del numero medio di vicini per le metriche.
\end{enumerate}

% ---------------------------------------------------------------------
\subsection{Il ciclo a eventi discreti}
\label{sec:eventloop}
La coda degli eventi \`e una coda di priorit\`a realizzata con uno \emph{heap} binario.
Ogni inserimento accoda la tripla
$(t,\, c,\, e)$ --- istante, contatore, evento --- dove $c$ \`e un contatore globale incrementato a
ogni accodamento: a parit\`a di istante gli eventi vengono cos\`i estratti
nell'ordine in cui sono stati accodati (FIFO). Questo dettaglio rende l'esecuzione
\emph{deterministica} a parit\`a di seed, requisito essenziale per la riproducibilit\`a
degli esperimenti. 

Il ciclo principale \`e il classico loop di un
simulatore a eventi discreti: estrae l'evento pi\`u imminente, fa avanzare il tempo
simulato al suo istante e invoca l'handler dell'entit\`a
destinataria. Il tempo
quindi non scorre in passi fissi ma ``salta'' da un evento al successivo.
Il ciclo termina quando il tempo simulato supera la durata configurata oppure quando la coda degli eventi rimane vuota.

Eventuali errori sollevati da un handler vengono catturati e registrati nei log senza interrompere la
simulazione; in questo modo un errore isolato non invalida l'intera esecuzione degli esperimenti.

% ---------------------------------------------------------------------
\subsection{Dinamicit\`a della topologia di rete}
\label{sec:dynamics}
La topologia della rete varia durante la simulazione ed \`e gestita dall'handler dell'evento \texttt{BUOY\_ARRAY\_UPDATE}. 
La logica di queste variazioni dipende dallo scenario configurato:  
\begin{itemize}[leftmargin=1.4em,nosep]
  \item \textbf{static} --- tutte le boe sono attive dall'inizio alla fine; l'evento non
  viene mai schedulato;
  \item \textbf{ramp} --- si parte con sole $2$ boe attive e se ne attiva una alla volta a
  intervallo fisso, calcolato spalmando le attivazioni rimanenti sulla durata residua della simulazione $d$: 
  \begin{equation}
      \mathcal{I} = \frac{|Boe| - 2}{d}\,\si{\second};
  \label{eq:ramp}
  \end{equation}
  serve a osservare il comportamento dei protocolli al crescere
  monotono della densit\`a;
  \item \textbf{random} --- a ogni aggiornamento, rischedulato a intervallo casuale
  $\mathcal{U}[15, 20]\,\si{\second}$, viene attivato o disattivato --- con probabilit\`a
  $50\%$ e quando \`e possibile farlo in base al numero minimo di boe ---
  un numero casuale di boe fino a un massimo di $\max(1, \lfloor \rho\,N \rfloor)$,
  con $N$ il totale delle boe e $\rho$ la percentuale di variabilit\`a configurata; 
  vi \`e anche un vincolo inferiore sul numero minimo di boe che devono rimanere attive, pari a $\max(3,\, 0.2\,N)$.
\end{itemize}

L'attivazione di una boa ne rischedula gli eventi ricorrenti (verifica dello scheduler,
pulizia dei vicini e il movimento per le boe mobili). La disattivazione pone il problema
opposto: gli eventi gi\`a in coda per quella boa non possono essere rimossi da uno heap in
modo efficiente. Si \`e adottato perci\`o un sistema di \emph{cancellazione pigra}: ogni boa mantiene un
contatore di generazione, incrementato a ogni disattivazione e incluso
negli eventi che essa schedula (Sezione~\ref{sec:event}). Quando un evento viene
estratto dalla coda, la boa lo scarta se \`e inattiva o se la generazione dell'evento non
coincide pi\`u con la sua attuale: gli eventi ``vecchi'' vengono cos\`i ignorati, senza costi
di rimozione. Alla disattivazione vengono inoltre svuotate la coda di inoltro
e la relativa memoria di deduplicazione (Sezione~\ref{sec:forward-impl}), cos\`i una boa che
rientra in rete non inoltra informazioni obsolete.

% ---------------------------------------------------------------------
\section{Modellazione della boa}
\label{sec:buoy}
La boa \`e la componente pi\`u complessa del simulatore, implementa: la politica CSMA/CA di 
accesso al mezzo, la creazione e la ricezione dei beacon e il modello di mobilit\`a RWP (Sezione~\ref{sec:rwp}).
Gestisce inoltre il funzionamento delle due modalit\`a di multihop descritte nel Capitolo~\ref{ch:contributo}, la cui implementazione \`e
approfondita nella sezione dedicata (Sezione~\ref{sec:multihop-impl}). 
Il suo comportamento \`e organizzato come una macchina a stati finiti
(enum \texttt{BuoyState}): \texttt{RECEIVING} \`e lo stato di riposo in cui la boa
ascolta il canale, \texttt{WAITING\_DIFS} e \texttt{BACKOFF} sono gli stati intermedi
della contesa del mezzo, mentre \texttt{SLEEPING} \`e riservato alle boe disattivate
dagli scenari dinamici (\emph{ramp} e \emph{random}, Sezione~\ref{sec:dynamics}).

% ---------------------------------------------------------------------
\subsection{Pipeline di trasmissione}
\label{sec:txpipeline}
La Figura~\ref{fig:tx-pipeline} mostra il percorso completo che segue una boa prima di iniziare la trasmissione di un frame.
Il punto di partenza \`e l'evento periodico \texttt{SCHEDULER\_CHECK}: la boa interroga
il proprio \texttt{BeaconScheduler} circa l'intervallo da attendere prima di trasmettere; esso ne stabilisce il tipo in base al protocollo attivo: fisso per SBP; dinamico per ADAB e
ACAB, calcolato rispettivamente con le Eq.~\ref{eq:adab} e~\ref{eq:acab-blend} (Capitolo~\ref{ch:risultati}). 
Terminata l'attesa, la boa registra la volont\`a di trasmettere e, se non
c'\`e gi\`a una contesa in corso, parte la pipeline CSMA/CA. Un apposito indicatore
di contesa in corso, che agisce da \emph{lock}, garantisce che ogni boa gestisca al pi\`u una
pipeline alla volta, a prescindere dal numero di beacon propri o di inoltri in attesa.

La contesa segue i principi della DCF~\cite{sharetechnote-dcf,csma-ca} richiamati nel Capitolo~\ref{ch:contributo}:
\begin{enumerate}[leftmargin=1.6em,nosep]
  \item \texttt{CHANNEL\_SENSE}: la boa sonda il canale tramite la callback
  di carrier sensing (Sezione~\ref{sec:files}); se \`e occupato, il canale stesso restituisce l'istante in
  cui torner\`a libero (per simulare efficacemente il sensing del canale) e la boa rischedula l'evento esattamente per quell'istante;
  \item \texttt{DIFS\_COMPLETION}: a canale libero la boa attende il tempo DIFS e
  riverifica il canale; se nel frattempo si \`e occupato, si ritorna al punto 1;
  \item \texttt{BACKOFF\_COMPLETION}: superato il DIFS, la boa calcola un backoff casuale:
  \begin{equation}
    T_{\text{backoff}} = b \cdot T_{\text{slot}},
    \qquad b \sim \mathcal{U}\{0, \dots, CW-1\},
    \label{eq:backoff}
  \end{equation}
  con $CW = 16$ e $T_{\text{slot}} = \SI{20}{\micro\second}$
  (DIFS $= \SI{50}{\micro\second}$; valori impostati nella
  configurazione). Al termine dell'attesa il canale viene verificato un'ultima volta:
  se risulta occupato si rientra dal punto 1 con un backoff nuovo, altrimenti si
  procede alla trasmissione;
  \item \texttt{TRANSMISSION\_START}: la boa invia \emph{un solo frame} per ogni contesa
  vinta. I beacon propri hanno la precedenza: se la boa ha un beacon proprio in attesa viene
  creato e trasmesso un beacon nuovo, altrimenti viene inoltrato il primo beacon della
  coda di inoltro con il TTL decrementato (Eq.~\ref{eq:ttl},
  Sezione~\ref{sec:forward}). Se dopo l'invio la coda di
  inoltro contiene ancora qualcosa, la boa rientra immediatamente in contesa dal punto 1; in caso
  contrario rilascia il lock e torna in ascolto.
\end{enumerate}
A ogni stadio della pipeline \`e ripetuto un controllo di uscita: se non resta nulla da
inviare (n\'e beacon proprio n\'e inoltri) il lock viene rilasciato e la pipeline termina definitivamente. 
Ai fini delle metriche, la boa registra anche la \emph{latenza dello scheduler}:
il tempo trascorso tra la decisione di trasmettere e l'effettivo inizio della
trasmissione, ossia il costo della contesa CSMA/CA.

\begin{figure}[p]
  \centering
  \includegraphics[height=0.86\textheight,keepaspectratio]{main/images/tx_pipeline.pdf}
  \caption[Pipeline di trasmissione della boa]{%
    Pipeline di trasmissione di una boa (CSMA/CA). La pipeline ha due punti d'ingresso: la decisione
    periodica dello scheduler (\texttt{SCHEDULER\_CHECK}) e l'accodamento di
    un beacon inoltrabile alla ricezione (\texttt{RECEPTION}); in entrambi i
    casi la contesa parte solo se non ce n'\`e gi\`a una in corso (lock di
    contesa). Il canale viene rilevato prima del DIFS, dopo di
    esso e al termine del backoff (Eq.~\ref{eq:backoff}): se risulta occupato
    in una qualunque delle tre verifiche, si rientra dal \texttt{CHANNEL\_SENSE}.
    Vinta la contesa, la boa invia un solo frame -- con precedenza al beacon
    proprio, altrimenti un inoltro dalla coda con il TTL decrementato
    (Eq.~\ref{eq:ttl}) -- e, se restano inoltri in sospeso, rientra subito in
    contesa.}
  \label{fig:tx-pipeline}
\end{figure}

% ---------------------------------------------------------------------
\subsection{Ricezione di un beacon}
\label{sec:rx}
L'handler dell'evento \texttt{RECEPTION} scarta anzitutto i beacon la cui consegna \`e stata
revocata da una collisione sopravvenuta da un terminale nascosto~\cite{tobagi-hidden} (il meccanismo \`e descritto nella
Sezione~\ref{sec:broadcast-impl}). Per i beacon ancora validi aggiorna la tabella dei vicini
diretti con il mittente e il timestamp dell'ultimo contatto, usato poi per i calcoli di ACAB (Eq.~\ref{eq:acab-con}). 
Ogni ricezione popola l'insieme dei nodi unici conosciuti dalla boa --- il mittente, l'eventuale origine (per i pacchetti del forwarding singolo) e tutti i nodi
della lista annunciata --- da cui deriva la metrica di scoperta della rete
(Eq.~\ref{eq:netdisc}). La gestione specifica del beacon al variare della
modalit\`a multihop attiva \`e descritta nella Sezione~\ref{sec:multihop-impl}.

% ---------------------------------------------------------------------
\subsection{Modello di mobilit\`a Random Waypoint}
\label{sec:rwp}
Una frazione configurabile delle boe (il $30\%$ negli
esperimenti) \`e mobile e si muove secondo il modello \emph{Random Waypoint} (RWP)~\cite{rwp},
gestito dall'evento \texttt{BUOY\_MOVEMENT}. Il modello genera
traiettorie che alternano tratte rettilinee e soste; ogni boa si muove in modo
indipendente dalle altre e ci\`o \`e una buona approssimazione
del comportamento di nuotatori e piccoli mezzi che si spostano ognuno per conto proprio.

Prima di intraprendere una nuova tratta, la boa sceglie casualmente la destinazione (\emph{waypoint}) $W$,
uniformemente distribuita nel piano di simulazione, e la velocit\`a $v$, vincolata secondo l'intervallo configurato:
\begin{equation}
  W \sim \mathcal{U}\big([0, L_x] \times [0, L_y]\big),
  \qquad
  v \sim \mathcal{U}[v_{\min},\, v_{\max}],
  \label{eq:rwp-draw}
\end{equation}
con $L_x,L_y$ le dimensioni dei lati del piano e $v_{\min},v_{\max}$ rispettivamente le velocit\`a minima e massima stabilite. 
Il moto verso il waypoint \`e calcolato a intervalli discreti di
durata $\Delta t$ di tempo simulato: detta $\mathbf{p}_t$ la
posizione corrente, a ogni passo si ricalcolano il versore di direzione, il vettore
velocit\`a e la nuova posizione:
\begin{equation}
  \hat{\mathbf{u}}_t = \frac{W - \mathbf{p}_t}{\lVert W - \mathbf{p}_t \rVert},
  \qquad
  \mathbf{v}_t = v\,\hat{\mathbf{u}}_t,
  \qquad
  \mathbf{p}_{t+\Delta t} = \mathbf{p}_t + \mathbf{v}_t\,\Delta t .
  \label{eq:rwp-step}
\end{equation}
Il modulo della velocit\`a resta costante lungo tutta la tratta, e il vettore
$\mathbf{v}_t$ non serve solo al movimento: \`e anche la componente di
mobilit\`a di ACAB (Eq.~\ref{eq:acab-mob}). Quando la distanza residua \`e coperta da un
solo passo, cio\`e $\lVert W - \mathbf{p}_t \rVert \le v\,\Delta t$, la boa si posiziona
esattamente al waypoint e azzera la velocit\`a; da qui decide il prossimo waypoint e
la prossima velocit\`a (Eq.~\ref{eq:rwp-draw}) e sosta per una pausa casuale
\begin{equation}
  \tau \sim \mathcal{U}[\tau_{\min},\, \tau_{\max}],
  \qquad
  [\tau_{\min}, \tau_{\max}] = [0, 10]\,\si{\second},
  \label{eq:rwp-pause}
\end{equation}
scaduta la quale ricomincia il processo con la tratta successiva.
Il prossimo evento \texttt{BUOY\_MOVEMENT} viene fissato direttamente all'istante in cui la pausa
scade. Durante la sosta la velocit\`a nulla azzera la componente di mobilit\`a di
ACAB (Eq.~\ref{eq:acab-mob}), che percepisce il nodo come fermo e ne rallenta la trasmissione dei beacon.

% ---------------------------------------------------------------------
\section{Implementazione delle modalit\`a multihop}
\label{sec:multihop-impl}
Questa sezione descrive la realizzazione delle due modalit\`a multihop \emph{forwarding singolo} e
\emph{forwarding aggregato} --- il contributo principale di questa tesi --- la cui logica
\`e stata definita nel Capitolo~\ref{ch:contributo} (Sezioni~\ref{sec:forward}
e~\ref{sec:append}). Entrambe sono gestite nella classe \texttt{Buoy} e, a seconda della modalit\`a attivata
(nessun multihop, forwarding singolo o forwarding aggregato),
si alterano due comportamenti del nodo: la creazione del beacon
e la sua ricezione. La contesa CSMA/CA
(Sezione~\ref{sec:txpipeline}), il canale e la raccolta delle metriche restano
identici nelle tre modalit\`a. Questo isolamento dei comportamenti garantisce che 
ogni differenza osservata negli esperimenti sia attribuibile esclusivamente
alla modalit\`a scelta.

% ---------------------------------------------------------------------
\subsection{Strutture dati dedicate}
\label{sec:multihop-state}
Oltre alla tabella dei vicini diretti --- comune a tutte le
modalit\`a ---, che associa a ogni vicino la coppia (timestamp dell'ultimo contatto,
posizione), ogni boa mantiene tre strutture specifiche per i multihop:
\begin{itemize}[leftmargin=1.4em,nosep]
  \item la \emph{memoria di deduplicazione} (forwarding singolo), che previene il \emph{broadcast storm}~\cite{broadcaststorm}: per
  ogni origin id si conserva il timestamp pi\`u recente del pacchetto gi\`a accettato per l'inoltro (Eq.~\ref{eq:dedup});
  \item la \emph{coda di inoltro} (forwarding singolo),
  indicizzata per \emph{origin id}: si comporta da coda
  FIFO, con la differenza che per ogni origine pu\`o esistere al massimo una
  copia in attesa. Quando arriva un beacon pi\`u fresco di uno gi\`a accodato dalla
  stessa origine, la voce viene sovrascritta sul posto conservando la posizione in
  coda: \`e ci\`o che garantisce la politica di sostituzione descritta nella
  Sezione~\ref{sec:forward};
  \item la \emph{tabella dei nodi scoperti} (forwarding aggregato), introdotta nella
  Sezione~\ref{sec:append}: associa a ogni nodo appreso
  indirettamente la tripla (timestamp, posizione, distanza in hop).
\end{itemize}

% ---------------------------------------------------------------------
\subsection{Modalit\`a forwarding singolo: accodamento e inoltro}
\label{sec:forward-impl}
In fase di creazione del beacon, la lista annunciata resta quella dei soli vicini diretti, ma
vengono valorizzati i campi \emph{origin id} (con l'identificatore della boa stessa) e
\emph{hop limit} (con il TTL iniziale stabilito in configurazione).

L'accodamento avviene alla ricezione secondo tre filtri a cascata: il
beacon non deve essere stato originato dalla boa stessa (al fine di evitare cicli di inoltro), 
deve avere TTL positivo e deve essere pi\`u fresco
dell'ultimo gi\`a accettato dalla stessa origine (Eq.~\ref{eq:dedup}, confronto con
la memoria di deduplicazione). Superati questi controlli, la voce entra in coda se c'\`e
posto oppure se l'origine \`e gi\`a presente; in quest'ultimo caso si sostituisce la copia pi\`u
vecchia. Allo stesso tempo, la decisione viene registrata nella memoria di
deduplicazione. A coda piena il beacon viene invece scartato \emph{senza} registrare
la decisione, cosicch\'e una copia successiva possa entrare se nel frattempo si \`e
liberato spazio.

L'inoltro \`e integrato nella stessa pipeline CSMA/CA dei beacon propri
(Sezione~\ref{sec:txpipeline}): non esiste un percorso di trasmissione privilegiato
per gli inoltri. Se al momento dell'accodamento la boa non \`e impegnata in una pipeline, ne
avvia una dopo il jitter casuale $\delta \sim \mathcal{U}[0, \SI{0.05}{\second}]$ che
desincronizza i nodi ricevitori dello stesso frame, i quali altrimenti
contenderebbero il canale tutti insieme. A ogni contesa vinta viene trasmesso un solo
frame, con precedenza ai beacon propri; quando \`e il turno di un inoltro, la boa
estrae la voce in testa alla coda e ne costruisce una copia
in cui la boa inoltrante ne diviene il nuovo mittente inserendovi la propria posizione ---
necessaria al canale per calcolare portata e propagazione --- mentre origine,
timestamp e lista dei vicini restano immutati e il TTL viene decrementato
(Eq.~\ref{eq:ttl}). Se dopo l'invio la coda non \`e vuota, la boa rientra
immediatamente in contesa senza rilasciare il lock; altrimenti la
pipeline termina e la boa torna in ascolto.

% ---------------------------------------------------------------------
\subsection{Modalit\`a forwarding aggregato: annuncio e integrazione delle liste}
\label{sec:append-impl}
In fase di creazione, il beacon annuncia i vicini diretti della boa mittente, ciascuno a
distanza di hop $1$; in modalit\`a forwarding aggregato la lista di questi viene estesa con l'intero contenuto
della tabella dei nodi scoperti, ciascuna voce con la distanza in hop registrata al
momento della scoperta (Eq.~\ref{eq:append-hop}). Il costo di questa modalit\`a \`e determinato
dalla lista dei vicini pi\`u lunga che accresce proporzionalmente la dimensione del frame (Eq.~\ref{eq:beacon-size}).
Il canale ne converte poi il peso in bit
in tempo di occupazione del mezzo (Eq.~\ref{eq:txtime}).

Alla ricezione, l'integrazione fatta nella lista annunciata
segue quattro passi, nell'ordine:
\begin{enumerate}[leftmargin=1.6em,nosep]
  \item il mittente diventa vicino diretto e viene quindi rimosso dalla
  tabella dei nodi scoperti, mantenendo la
  disgiunzione $\{\emph{nodi\,vicini}\}\cap\{\emph{nodi\,scoperti}\} = \varnothing$
  descritta nella Sezione~\ref{sec:append};
  \item le voci che coincidono con il ricevente stesso o con un suo vicino diretto
  vengono ignorate;
  \item la distanza in hop di ogni voce filtrata viene incrementata di uno
  (Eq.~\ref{eq:append-hop});
  \item la voce viene scritta in tabella solo se pi\`u fresca di quella in memoria
  (Eq.~\ref{eq:append-fresh}); il caso del nodo mai visto prima \`e gestito con un
  timestamp sentinella pari a $-\infty$, che rende la condizione sempre
  vera al primo incontro.
\end{enumerate}
Il costo computazionale per beacon ricevuto \`e lineare nella lunghezza della lista
annunciata.

% ---------------------------------------------------------------------
\subsection{Aggiornamento delle memorie}
\label{sec:multihop-cleanup}
Le strutture accumulate dalle due modalit\`a non crescono senza controllo. L'evento
periodico \texttt{NEIGHBOR\_CLEANUP} rimuove da ogni struttura dati ogni voce pi\`u vecchia della scadenza configurata:
nel forwarding singolo sia le decisioni di inoltro ormai stantie nella memoria di deduplicazione,
sia i beacon rimasti troppo a lungo nella coda di inoltro; nel forwarding aggregato le voci della tabella dei nodi scoperti.
Alla disattivazione di una boa negli scenari dinamici, coda di inoltro e memoria di
deduplicazione vengono inoltre svuotate per intero, per il motivo spiegato nella Sezione~\ref{sec:dynamics}.

% ---------------------------------------------------------------------
\section{Funzionamento del canale}
\label{sec:channel}
Il canale \`e il mezzo condiviso attraverso cui viaggiano tutti i beacon. Mantiene la
lista delle \emph{trasmissioni attive} --- contenente le triple $(\text{beacon},\, t_{\text{inizio}},\,
t_{\text{fine}})$ ---, con la quale gestisce sia il carrier sensing delle boe sia il rilevamento delle collisioni.

% ---------------------------------------------------------------------
\subsection{Rilevamento del canale libero}
\label{sec:carrier-sense}
Il carrier sensing stabilisce se il canale \`e occupato \emph{localmente}, nel
punto in cui si trova la boa richiedente. Per ogni trasmissione attiva il cui mittente \`e entro la portata massima, il segnale
raggiunge quella posizione con un ritardo di propagazione $d/c$ ($d$ la
distanza dal mittente, $c$ la velocit\`a di propagazione) e la occupa nell'intervallo
\begin{equation}
  \Big[\, t_{\text{inizio}} + \tfrac{d}{c},\;\; t_{\text{fine}} + \tfrac{d}{c} \,\Big].
  \label{eq:busy-window}
\end{equation}
Il canale risulta occupato se l'istante corrente si interseca con almeno una di queste finestre. 
Oltre al verdetto, il canale restituisce anche l'istante massimo in cui il mezzo torner\`a
libero in quel punto; in tal modo la boa rischedula il proprio \texttt{CHANNEL\_SENSE} esattamente a
quell'istante, evitando nuovi sensing inutili. Le trasmissioni ormai esaurite vengono
rimosse dalla lista
filtrandole secondo il tempo di fine trasmissione di ciascuna ($t_{\text{fine}}$),
tenendo conto di un piccolo periodo di grazia approssimato che copre il tempo di propagazione residuo.


% ---------------------------------------------------------------------
\subsection{Trasmissione broadcast}
\label{sec:broadcast-impl}
Quando una boa vince la contesa, il canale prende in carico il frame.
La durata della trasmissione dipende dalla dimensione del beacon $S(k)$
(Eq.~\ref{eq:beacon-size}) e dal bit rate $R$ del canale:
\begin{equation}
  T_{\text{tx}} = \frac{S(k)}{R},
  \qquad R = \SI{1}{Mbit/s},
  \label{eq:txtime}
\end{equation}
quindi un beacon che annuncia molti nodi occupa il mezzo pi\`u a lungo; ci\`o \`e il costo
in airtime della modalit\`a forwarding aggregato discusso nella Sezione~\ref{sec:tradeoff}. La
trasmissione viene registrata tra quelle attive e i destinatari sono tutte le boe attive
entro la portata massima dal mittente; per ciascun destinatario eleggibile alla ricezione viene schedulato
un evento \texttt{RECEPTION} all'istante $t_{\text{fine}} + d/c$.

\paragraph{Collisioni e terminale nascosto.} Prima di schedulare le ricezioni, il canale
confronta il nuovo frame con ogni trasmissione attiva la cui finestra temporale si
sovrappone alla sua. Si distinguono due casi:
\begin{itemize}[leftmargin=1.4em,nosep]
  \item \textbf{collisione diretta}: i mittenti sono nel raggio l'uno dell'altro
  (evento raro, perch\'e il carrier sensing tende a impedirlo), di conseguenza il nuovo beacon \`e
  perso per tutti i suoi destinatari;
  \item \textbf{terminale nascosto}: i mittenti sono fuori portata reciproca --- quindi
  il carrier sensing non poteva rilevare nulla --- ma un destinatario del nuovo beacon
  \`e nel raggio di entrambi. Presso quel destinatario i due segnali si sovrappongono:
  il nuovo beacon \`e perso, e anche la ricezione del beacon preesistente, se era gi\`a
  stata schedulata per quel nodo, viene \emph{revocata}.
\end{itemize}
La revoca sfrutta un insieme di destinatari associato a ciascun beacon
come struttura dati di simulazione (non fa parte del pacchetto on-air): il canale vi
registra i destinatari con una consegna valida e una collisione successiva ne rimuove la
voce. Durante la \texttt{RECEPTION} --- evento che era gi\`a in coda e perci\`o non pi\`u rimovibile --- 
la boa destinataria non trova il proprio id nell'insieme e quindi non processa nulla: \`e l'equivalente
simulativo di un nodo che riceve bit corrotti.

\paragraph{Perdita probabilistica con canale non ideale.} Se il canale \`e configurato
come \emph{non ideale}, ogni consegna sopravvissuta alle collisioni viene
messa alla prova da una probabilit\`a dipendente dalla distanza:
\begin{equation}
  P_{\text{rx}}(d) =
  \begin{cases}
    p_{\text{high}} = 0.9 & d \le r_{\text{high}} = \SI{70}{\metre},\\[2pt]
    p_{\text{low}} = 0.15 & r_{\text{high}} < d \le r_{\text{max}} = \SI{120}{\metre},\\[2pt]
    0 & d > r_{\text{max}},
  \end{cases}
  \label{eq:delivery-prob}
\end{equation}
Queste probabilit\`a derivano da misurazioni effettuate sul campo~\cite{prosafe}: entro i \SI{70}{\metre} la consegna
\`e affidabile, oltre questo raggio il segnale \`e fortemente ridotto e la maggior parte dei
frame va persa, oltre la portata massima (\SI{120}{\metre}) non arriva pi\`u nulla. I frame respinti da questo errore probabilistico
sono conteggiati come perdite di canale, distinte dalle perdite per collisione.

% ---------------------------------------------------------------------
\section{Parametri di configurazione}
\label{sec:config}
Tutti i parametri della simulazione risiedono in un unico file di configurazione,
organizzato in sezioni per categoria di competenza: la simulazione (durata, scenario,
modalit\`a multihop), il piano di simulazione (dimensioni), le boe (mobilit\`a), la rete
(bit rate, portate e probabilit\`a di consegna), la contesa CSMA/CA (tempi di accesso al
mezzo) e i protocolli di beaconing (intervalli e soglie).

La classe \texttt{ConfigHandler} \`e un \emph{singleton}: la configurazione viene
caricata una sola volta per processo in esecuzione e ogni componente legge i propri
parametri direttamente all'atto della costruzione, senza che i valori debbano essere
passati esplicitamente. In assenza del file, la configurazione viene rigenerata a
partire da valori di default.

L'avvio degli esperimenti coinvolge due componenti. Un lanciatore di batch
legge la configurazione, costruisce il piano degli esperimenti come combinazioni
di protocolli, modalit\`a multihop, densit\`a e intervalli di beaconing, genera
\emph{una sola volta} le posizioni iniziali per ciascuna densit\`a --- cos\`i ogni
combinazione confronta le stesse topologie --- e deriva da un seed principale un seed
riproducibile per ogni run.
Le simulazioni vengono eseguite in parallelo come sottoprocessi; ciascun sottoprocesso
costruisce concretamente la propria simulazione: istanzia metriche,
canale e boe (ciascuna con il proprio \texttt{BeaconScheduler}), collega le callback
descritte nella Sezione~\ref{sec:files} e avvia il \texttt{Simulator}, esportando le
metriche al termine. La Tabella~\ref{tab:config} riporta i principali parametri di
configurazione con i valori impiegati negli esperimenti.

\begin{table}[htbp]
  \centering
  \caption[Parametri di configurazione]{Principali parametri di configurazione e valori
  impiegati negli esperimenti.}
  \label{tab:config}
  \small
  \begin{tabular}{@{}p{0.66\textwidth}l@{}}
    \toprule
    \textbf{Parametro} & \textbf{Valore} \\
    \midrule
    \multicolumn{2}{@{}l}{\emph{Simulazione}}\\
    Protocolli di beaconing simulati & SBP, ADAB, ACAB \\
    Densit\`a di boe (minima--massima, passo) & $20$--$100$, passo $20$ \\
    Intervalli di beaconing $BI_{\text{static}}$ [s] & $1.0$, $0.5$, $0.25$ \\
    Durata di ogni simulazione [s] & $200$ \\
    Simulazioni eseguite in parallelo & $10$ \\
    Canale & ideale e non ideale (due scenari) \\
    Scenario & \emph{random} \\
    Percentuale di variabilit\`a $\rho$ dello scenario \emph{random} & $0.30$ \\
    Modalit\`a multihop simulate & nessuna, aggregato, singolo \\
    TTL iniziale dei beacon & $5$ \\
    Capienza della coda di inoltro & $50$ \\
    Limite di hop del forwarding aggregato (non attivo, Sez.~\ref{sec:append-hoplimit}) & $2$ \\
    Baseline $n_0$ del gate di forwarding (non attivo, Sez.~\ref{sec:fwdgate}) & $5$ \\
    \midrule
    \multicolumn{2}{@{}l}{\emph{Piano di simulazione}}\\
    Dimensioni del piano [m] & $600 \times 600$ \\
    \midrule
    \multicolumn{2}{@{}l}{\emph{Boe}}\\
    Percentuale di boe mobili & $30\%$ \\
    Velocit\`a di riferimento $v_{\text{ref}}$ di ACAB [m/s] & $15$ \\
    RWP: velocit\`a per tratta $[v_{\min},\, v_{\max}]$ [m/s] & $[5, 20]$ \\
    RWP: pausa al waypoint $[\tau_{\min},\, \tau_{\max}]$ [s] & $[0, 10]$ \\
    \midrule
    \multicolumn{2}{@{}l}{\emph{Rete}}\\
    Bit rate del canale [Mbit/s] & $1$ \\
    Velocit\`a di propagazione del segnale [m/s] & $3 \cdot 10^{8}$ \\
    Portata massima $r_{\max}$ [m] & $120$ \\
    Soglia di consegna affidabile $r_{\text{high}}$ [m] & $70$ \\
    Probabilit\`a di consegna entro $r_{\text{high}}$ & $0.9$ \\
    Probabilit\`a di consegna tra $r_{\text{high}}$ e $r_{\max}$ & $0.15$ \\
    \midrule
    \multicolumn{2}{@{}l}{\emph{Contesa CSMA/CA}}\\
    Durata dello slot di backoff [\si{\micro\second}] & $20$ \\
    Durata del DIFS [\si{\micro\second}] & $50$ \\
    Contention window $CW$ & $16$ \\
    \midrule
    \multicolumn{2}{@{}l}{\emph{Protocolli di beaconing}}\\
    $BI_{\min}$ dei protocolli adattivi [s] & $0.25$ \\
    $BI_{\max}$ dei protocolli adattivi [s] & $5.0$ \\
    Scadenza delle voci nelle tabelle [s] & $7.0$ \\
    $N_{\text{thr}}$ di ADAB (Eq.~\ref{eq:adab-density}) & $15$ \\
    $N_{\text{thr}}$ di ACAB (Eq.~\ref{eq:acab-den}) & $10$ \\
    $\tau_c$ di ACAB [s] (Eq.~\ref{eq:acab-con}) & $20$ \\
    Pesi $(w_d,\, w_c,\, w_m)$ di ACAB (Eq.~\ref{eq:acab-blend}) & $(0.4,\, 0.3,\, 0.3)$ \\
    \bottomrule
  \end{tabular}
\end{table}

% ---------------------------------------------------------------------
\section{Raccolta delle metriche}
\label{sec:metrics}
La classe \texttt{Metrics} accumula i dati della simulazione provenienti dalle altre componenti: 
il canale registra gli esiti di ogni trasmissione (destinatari potenziali, consegne, collisioni, perdite) e le boe riportano le ricezioni e
i nodi scoperti tramite le callback assegnate all'avvio. A questi flussi si aggiunge il
campionamento periodico schedulato con l'evento \texttt{AVG\_NEIGHBORS\_CALCULATION}: il simulatore calcola il
numero medio di vicini costruendo un \emph{k-d tree}~\cite{kdtree} sulle posizioni delle boe
attive e contando le coppie entro la portata massima, con costo $O(n \log n)$. 
Se $P$ \`e l'insieme delle coppie in portata e $n$ le boe attive, la media \`e
$2\,|P| / n$, poich\'e ogni coppia rappresenta due distinte relazioni di vicinato.
Al termine delle simulazioni le metriche vengono esportate in appositi file CSV:
un elenco dei risultati per ogni combinazione di parametri e la serie temporale della scoperta dei nodi.
Le metriche derivate da questi dati grezzi, con le rispettive formule di
calcolo, sono definite nella Sezione~\ref{sec:eval-metrics} del
Capitolo~\ref{ch:risultati}, a introduzione della valutazione sperimentale.

% ---------------------------------------------------------------------
\section*{Sintesi del capitolo}
Questo capitolo ha presentato il simulatore a eventi discreti: la coda di priorit\`a
con ordinamento deterministico; la macchina a stati DCF della boa con la sua pipeline
di trasmissione; l'implementazione delle due modalit\`a multihop che
costituiscono il contributo della tesi; il modello di canale calibrato
sul campo, con propagazione, collisioni dirette e da terminale nascosto, e perdita per errore del mezzo; 
il modello di mobilit\`a Random Waypoint; la gestione della popolazione
dinamica; la configurazione centralizzata e la raccolta delle metriche.
Il simulatore offre una piattaforma controllata e riproducibile per valutare i
protocolli di broadcast. Il prossimo capitolo presenta la metodologia sperimentale e i
risultati.
